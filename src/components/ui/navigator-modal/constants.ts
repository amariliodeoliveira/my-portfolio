// Shared by every clickable row in the navigator modal (links and buttons
// alike) so their layout/hover styling can't drift apart. btn-ghost already
// gives the transparent-until-hovered look — no need to hand-roll it.
export const MENU_ITEM_CLASSNAME =
  "btn btn-ghost btn-block justify-between rounded-none";
